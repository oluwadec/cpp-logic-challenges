#include <iostream>
using namespace std;
int main() {
	double length;
	double width;
	double area;
	cin >> length;
	cin >> width;
	double area = length * width;
	cout << " The area of the rectangle is :" << area << endl;
	return 0;
}